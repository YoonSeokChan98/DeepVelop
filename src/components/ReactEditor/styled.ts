import styled from 'styled-components';

const ReactEditorStyled = styled.div`
    /* .options {
        display: flex;
        padding: 10px;
        .fontSize {
            display: flex;
            div {
                margin: 0 5px;
            }
            div:hover {
                color: red;
                background-color: #ccc;
                border-radius: 5px;
            }
        }
        .fontColor {
            display: flex;
            font-size: xx-small;
            .colorField {
                width: 3rem;
                padding: 2px;
            }
            div {
                margin: 0 5px;
            }
            div:hover {
                color: red;
                background-color: #ccc;
                border-radius: 5px;
            }
        }
    } */
`;

export default ReactEditorStyled;
