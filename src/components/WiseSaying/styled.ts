import styled from 'styled-components';

const WiseSayingStyled = styled.div`
.wiseBox{
    text-align: center;
    padding: 20px;
    font-size: large;
}
    /* @keyframes typing {
        from {
            width: 0;
        }
    }

    @keyframes caret {
        50% {
            border-right-color: transparent;
        }
    }
    .wiseBox {
        .wiseSaying {
            font: bold 200% Consolas, Monaco, monospace;
            width: 8.25em;
            width: 16ch;
            white-space: nowrap;
            overflow: hidden;
            border-right: 0.05em solid;
            animation: typing 8s steps(16), caret 1s steps(1) infinite;
        }
    } */
`;

export default WiseSayingStyled;
